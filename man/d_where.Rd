% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-verbs.R
\name{d_where}
\alias{d_where}
\title{Apply a boolean mask to a delayed matrix}
\usage{
d_where(x, predicate, fill = 0)
}
\arguments{
\item{x}{A \code{delarr}.}

\item{predicate}{A function or formula returning a logical matrix.}

\item{fill}{Replacement value for elements where the predicate is \code{FALSE}.}
}
\value{
A \code{delarr} including the mask.
}
\description{
Elements failing the predicate are replaced with \code{fill} at materialisation
time.
}
\examples{
mat <- matrix(c(-1, 2, -3, 4, -5, 6), nrow = 2, ncol = 3)
darr <- delarr(mat)

# Replace negative values with 0
masked <- darr |> d_where(~ .x >= 0, fill = 0) |> collect()
masked

# Replace values below threshold with NA
filtered <- darr |> d_where(~ .x > 1, fill = NA) |> collect()
filtered
}
