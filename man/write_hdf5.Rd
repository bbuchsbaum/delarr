% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-writer-hdf5.R
\name{write_hdf5}
\alias{write_hdf5}
\title{Write a matrix to an HDF5 file}
\usage{
write_hdf5(x, path, dataset, compression = 4L)
}
\arguments{
\item{x}{A matrix to write.}

\item{path}{Path to the HDF5 file. Created if it doesn't exist.}

\item{dataset}{Name of the dataset to create.}

\item{compression}{Gzip compression level (0-9), or NULL for no compression.}
}
\value{
The path to the HDF5 file (invisibly).
}
\description{
Simple convenience function to write a matrix to an HDF5 dataset. For
streaming writes during \code{collect()}, use \code{hdf5_writer()} instead.
}
\examples{
if (requireNamespace("hdf5r", quietly = TRUE)) {
  # Write a matrix to HDF5
  mat <- matrix(1:20, nrow = 4, ncol = 5)
  tf <- tempfile(fileext = ".h5")
  write_hdf5(mat, tf, "X")

  # Read it back as a delarr
  darr <- delarr_hdf5(tf, "X")
  collect(darr)

  # Clean up
  unlink(tf)
}
}
