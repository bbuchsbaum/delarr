---
phase: 02-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/delarr-eval.R
  - tests/testthat/test-reductions.R
autonomous: true

must_haves:
  truths:
    - "All-NA row reductions return NA vector (not NaN)"
    - "All-NA column reductions return NA vector (not NaN)"
    - "Mixed-NA reductions return correct values for non-NA positions"
  artifacts:
    - path: "R/delarr-eval.R"
      provides: "Fixed streaming reduction logic"
      contains: "acc\\[counts == 0\\] <- NA_real_"
    - path: "tests/testthat/test-reductions.R"
      provides: "All-NA edge case tests"
      exports: ["test_that"]
  key_links:
    - from: "tests/testthat/test-reductions.R"
      to: "R/delarr-eval.R"
      via: "d_reduce() calls"
      pattern: "d_reduce.*na.rm.*TRUE"
---

<objective>
Fix the all-NA reduction bug where mean/sum/min/max of all-NA inputs return NaN instead of NA, and add comprehensive tests verifying correct behavior.

Purpose: R convention dictates that reducing missing data should yield missing result (NA), not computational error (NaN). Current code violates this for edge cases.
Output: Fixed delarr-eval.R with NA handling for empty count sets, new test-reductions.R with edge case coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-quality/02-RESEARCH.md

@R/delarr-eval.R
@tests/testthat/test-core.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all-NA reduction handling in streaming collect()</name>
  <files>R/delarr-eval.R</files>
  <action>
Fix the streaming reduction logic in collect() to return NA (not NaN) when all values are NA:

1. **Row reductions (lines 315-376):**
   - For min/max: Initialize `acc <- NULL` stays, but after loop, set `acc[counts == 0] <- NA_real_` (already done at line 372-374)
   - Verify the pattern is consistent: sum (line 357-359), mean (line 363-366), min/max (line 372-374)
   - ISSUE: min() and max() with na.rm=TRUE on all-NA returns Inf/-Inf, not NA. Need to check counts AFTER reduction and override.

2. **Column reductions (lines 378-437):**
   - For min/max: Initialize `acc <- rep(NA_real_, n_cols)` (line 383) - this is correct start
   - ISSUE: apply(block, 2L, min, na.rm=TRUE) on all-NA column returns Inf, need to fix:
     - After the loop, add: `if (type %in% c("min", "max") && !is.null(counts) && na_rm) { acc[counts == 0] <- NA_real_ }`
   - Line 435-437 already has this pattern but verify it runs in all cases

3. **Key fix pattern:**
```r
# After accumulation loop, before return:
if (!is.null(counts) && na_rm) {
  acc[counts == 0] <- NA_real_
}
```

4. **Verify apply_reduce_full() (lines 114-138):**
   - This is used for generic reductions and full-eval path
   - base::min/max with na.rm=TRUE return Inf/-Inf on empty, need wrapper or post-processing
   - Add explicit check: after apply(), set result to NA where input row/col was all-NA

Note: Do NOT change behavior when na.rm=FALSE (should return NA naturally from R).
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
mat <- matrix(NA_real_, 3, 4)
x <- delarr(mat)

# Row reductions - all should be NA vectors, not NaN/Inf
collect(d_reduce(x, sum, "rows", na.rm = TRUE))   # expect: c(NA, NA, NA)
collect(d_reduce(x, mean, "rows", na.rm = TRUE))  # expect: c(NA, NA, NA)
collect(d_reduce(x, min, "rows", na.rm = TRUE))   # expect: c(NA, NA, NA) NOT Inf
collect(d_reduce(x, max, "rows", na.rm = TRUE))   # expect: c(NA, NA, NA) NOT -Inf

# Column reductions
collect(d_reduce(x, sum, "cols", na.rm = TRUE))   # expect: c(NA, NA, NA, NA)
collect(d_reduce(x, mean, "cols", na.rm = TRUE))  # expect: c(NA, NA, NA, NA)
collect(d_reduce(x, min, "cols", na.rm = TRUE))   # expect: c(NA, NA, NA, NA)
collect(d_reduce(x, max, "cols", na.rm = TRUE))   # expect: c(NA, NA, NA, NA)
```
  </verify>
  <done>All 8 reduction operations on all-NA matrix return NA vectors (verified with is.na() returning TRUE, is.nan() returning FALSE)</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive reduction edge case tests</name>
  <files>tests/testthat/test-reductions.R</files>
  <action>
Create new test file tests/testthat/test-reductions.R with comprehensive edge case coverage:

```r
# tests/testthat/test-reductions.R
# Edge case tests for d_reduce() operations

test_that("all-NA row reductions return NA not NaN/Inf", {
  mat <- matrix(NA_real_, 3, 4)
  x <- delarr(mat)

  # Test all standard reduction operations
  for (fn_name in c("sum", "mean", "min", "max")) {
    fn <- get(fn_name, envir = baseenv())
    result <- collect(d_reduce(x, fn, "rows", na.rm = TRUE))

    expect_true(all(is.na(result)),
                info = sprintf("%s(all-NA, na.rm=TRUE) rows should be NA", fn_name))
    expect_false(any(is.nan(result)),
                 info = sprintf("%s(all-NA) rows should not be NaN", fn_name))
    expect_false(any(is.infinite(result)),
                 info = sprintf("%s(all-NA) rows should not be Inf", fn_name))
  }
})

test_that("all-NA column reductions return NA not NaN/Inf", {
  mat <- matrix(NA_real_, 3, 4)
  x <- delarr(mat)

  for (fn_name in c("sum", "mean", "min", "max")) {
    fn <- get(fn_name, envir = baseenv())
    result <- collect(d_reduce(x, fn, "cols", na.rm = TRUE))

    expect_true(all(is.na(result)),
                info = sprintf("%s(all-NA, na.rm=TRUE) cols should be NA", fn_name))
    expect_false(any(is.nan(result)),
                 info = sprintf("%s(all-NA) cols should not be NaN", fn_name))
    expect_false(any(is.infinite(result)),
                 info = sprintf("%s(all-NA) cols should not be Inf", fn_name))
  }
})

test_that("mixed-NA reductions return correct values", {
  # First row all NA, second row has values, third row mixed
  mat <- matrix(c(NA, 1, NA,
                  NA, 2, 3,
                  NA, NA, 4,
                  NA, 5, NA), nrow = 3, ncol = 4)
  x <- delarr(mat)

  # Row sums with na.rm
  row_sums <- collect(d_reduce(x, sum, "rows", na.rm = TRUE))
  expect_equal(row_sums[1], NA_real_)  # All NA row
  expect_equal(row_sums[2], 1 + 2 + 5) # Has values
  expect_equal(row_sums[3], 3 + 4)     # Mixed

  # Row means with na.rm
  row_means <- collect(d_reduce(x, mean, "rows", na.rm = TRUE))
  expect_equal(row_means[1], NA_real_)
  expect_equal(row_means[2], mean(c(1, 2, 5)))
  expect_equal(row_means[3], mean(c(3, 4)))
})

test_that("chunked reductions match non-chunked for edge cases", {
  mat <- matrix(NA_real_, 4, 8)
  mat[2, ] <- 1:8  # One row with values
  x <- delarr(mat)

  # Compare different chunk sizes
  result_1 <- collect(d_reduce(x, sum, "rows", na.rm = TRUE), chunk_size = 1L)
  result_4 <- collect(d_reduce(x, sum, "rows", na.rm = TRUE), chunk_size = 4L)
  result_8 <- collect(d_reduce(x, sum, "rows", na.rm = TRUE), chunk_size = 8L)

  expect_equal(result_1, result_4)
  expect_equal(result_4, result_8)
  expect_equal(result_1[1], NA_real_)  # All-NA row
  expect_equal(result_1[2], sum(1:8))  # Row with values
})

test_that("reductions without na.rm return NA when any NA present", {

  mat <- matrix(c(1, NA, 3, 4), 2, 2)
  x <- delarr(mat)

  # Without na.rm, any NA propagates
  row_sums <- collect(d_reduce(x, sum, "rows", na.rm = FALSE))
  expect_true(is.na(row_sums[1]))  # Has NA
  expect_equal(row_sums[2], 3 + 4) # No NA
})
```
  </action>
  <verify>
```bash
cd /Users/bbuchsbaum/code/delarr && Rscript -e "devtools::test(filter = 'reductions')"
```
All tests pass.
  </verify>
  <done>test-reductions.R exists with 5+ test cases covering all-NA rows, all-NA cols, mixed-NA, chunked consistency, and na.rm=FALSE behavior</done>
</task>

</tasks>

<verification>
```bash
cd /Users/bbuchsbaum/code/delarr
Rscript -e "devtools::test(filter = 'reductions')"
Rscript -e "devtools::check()"
```

All reduction tests pass. R CMD check shows 0 errors, 0 warnings.
</verification>

<success_criteria>
1. All-NA reduction operations return NA (not NaN or Inf) for sum, mean, min, max
2. Behavior is consistent across row and column reductions
3. Chunked reductions match non-chunked results for edge cases
4. New test file test-reductions.R exists with comprehensive coverage
5. All tests pass on devtools::test()
6. R CMD check passes cleanly (0 errors, 0 warnings)
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-01-SUMMARY.md`
</output>
