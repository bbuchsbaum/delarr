---
phase: 01-baseline-documentation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/delarr-verbs.R
  - R/generics.R
autonomous: true

must_haves:
  truths:
    - "d_center() has runnable @examples showing row/column centering"
    - "d_scale() has runnable @examples showing scaling with options"
    - "d_zscore() has runnable @examples showing z-score normalization"
    - "d_detrend() has runnable @examples showing polynomial detrending"
    - "d_where() has runnable @examples showing masked updates"
    - "rowMeans2() has runnable @examples for delarr input"
    - "colMeans2() has runnable @examples for delarr input"
  artifacts:
    - path: "R/delarr-verbs.R"
      provides: "Transformation verb documentation with examples"
      contains: "@examples"
    - path: "R/generics.R"
      provides: "Generic function documentation with examples"
      contains: "@examples"
  key_links:
    - from: "d_center() example"
      to: "collect()"
      via: "materialization after transform"
      pattern: "collect\\("
---

<objective>
Add @examples to transformation verbs (d_center, d_scale, d_zscore, d_detrend, d_where) and generic functions (rowMeans2, colMeans2).

Purpose: Complete documentation for the 7 transformation/helper functions (DOCS-03).
Output: All 7 functions have runnable @examples demonstrating their primary use cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-RESEARCH.md
@R/delarr-verbs.R
@R/generics.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @examples to d_center(), d_scale(), d_zscore()</name>
  <files>R/delarr-verbs.R</files>
  <action>
Add @examples to d_center() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(c(1, 2, 3, 10, 20, 30), nrow = 2, ncol = 3)
#' darr <- delarr(mat)
#'
#' # Center rows (subtract row means)
#' centered_rows <- darr |> d_center(dim = "rows") |> collect()
#' centered_rows
#' rowMeans(centered_rows)  # Should be ~0
#'
#' # Center columns (subtract column means)
#' centered_cols <- darr |> d_center(dim = "cols") |> collect()
#' colMeans(centered_cols)  # Should be ~0
```

Add @examples to d_scale() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(c(1, 2, 3, 10, 20, 30), nrow = 2, ncol = 3)
#' darr <- delarr(mat)
#'
#' # Scale rows (center and divide by SD)
#' scaled <- darr |> d_scale(dim = "rows") |> collect()
#' scaled
#'
#' # Scale without centering
#' scaled_only <- darr |> d_scale(dim = "rows", center = FALSE) |> collect()
#' scaled_only
```

Add @examples to d_zscore() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(c(1, 2, 3, 10, 20, 30), nrow = 2, ncol = 3)
#' darr <- delarr(mat)
#'
#' # Z-score normalize rows
#' zscored <- darr |> d_zscore(dim = "rows") |> collect()
#' zscored
#'
#' # Row means should be ~0, row SDs should be ~1
#' rowMeans(zscored)
```

All examples use small 2x3 matrices for fast execution.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("d_center", package = "delarr")
example("d_scale", package = "delarr")
example("d_zscore", package = "delarr")
```
All should complete without error in <5 seconds.
  </verify>
  <done>d_center(), d_scale(), and d_zscore() have @examples that run without error.</done>
</task>

<task type="auto">
  <name>Task 2: Add @examples to d_detrend() and d_where()</name>
  <files>R/delarr-verbs.R</files>
  <action>
Add @examples to d_detrend() in R/delarr-verbs.R:

```r
#' @examples
#' # Create matrix with linear trend in rows
#' mat <- matrix(1:12 + rep(1:4, each = 3), nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#'
#' # Remove linear trend along rows
#' detrended <- darr |> d_detrend(dim = "rows", degree = 1L) |> collect()
#' detrended
#'
#' # Remove quadratic trend
#' quad_detrend <- darr |> d_detrend(dim = "rows", degree = 2L) |> collect()
#' quad_detrend
```

Add @examples to d_where() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(c(-1, 2, -3, 4, -5, 6), nrow = 2, ncol = 3)
#' darr <- delarr(mat)
#'
#' # Replace negative values with 0
#' masked <- darr |> d_where(~ .x >= 0, fill = 0) |> collect()
#' masked
#'
#' # Replace values below threshold with NA
#' filtered <- darr |> d_where(~ .x > 1, fill = NA) |> collect()
#' filtered
```

Examples demonstrate the key parameters (degree for detrend, predicate and fill for where).
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("d_detrend", package = "delarr")
example("d_where", package = "delarr")
```
Both should complete without error in <5 seconds.
  </verify>
  <done>d_detrend() and d_where() have @examples that run without error.</done>
</task>

<task type="auto">
  <name>Task 3: Add @examples to rowMeans2() and colMeans2() generics</name>
  <files>R/generics.R</files>
  <action>
Add @examples to rowMeans2() generic in R/generics.R:

```r
#' @examples
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#'
#' # Compute row means lazily
#' rowMeans2(darr)
#'
#' # Compare with base R
#' rowMeans(mat)
```

Add @examples to colMeans2() generic in R/generics.R:

```r
#' @examples
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#'
#' # Compute column means lazily
#' colMeans2(darr)
#'
#' # Compare with base R
#' colMeans(mat)
```

These examples show the matrixStats-compatible API.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("rowMeans2", package = "delarr")
example("colMeans2", package = "delarr")
```
Both should complete without error in <5 seconds.
  </verify>
  <done>rowMeans2() and colMeans2() have @examples that run without error.</done>
</task>

</tasks>

<verification>
- Run `devtools::document()` to regenerate .Rd files
- Run `devtools::check()` and verify no documentation warnings
- All 7 examples run without error: d_center(), d_scale(), d_zscore(), d_detrend(), d_where(), rowMeans2(), colMeans2()
- Each example completes in <5 seconds
</verification>

<success_criteria>
1. All 7 functions have @examples sections in their roxygen2 comments
2. `devtools::run_examples()` completes without error for these functions
3. Each example demonstrates the primary use case and key parameters
4. No files written outside tempdir() (none needed for in-memory examples)
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-03-SUMMARY.md`
</output>
