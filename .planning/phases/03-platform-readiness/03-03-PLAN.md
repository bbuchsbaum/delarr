---
phase: 03-platform-readiness
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "win-builder R-devel check passes with 0 errors, 0 warnings"
    - "win-builder R-release check passes with 0 errors, 0 warnings"
    - "All three backends (hdf5, mem, mmap) work on Windows"
  artifacts: []
  key_links:
    - from: "devtools::check_win_devel()"
      to: "win-builder email"
      via: "CRAN infrastructure"
      pattern: "win-builder"
---

<objective>
Validate package on Windows using win-builder service for both R-devel and R-release.

Purpose: CRAN requires packages to work on Windows. Win-builder is the official testing infrastructure used by CRAN maintainers. Testing here before submission catches platform-specific issues.

Output: Confirmation that package passes win-builder checks on both R-devel and R-release.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-readiness/03-CONTEXT.md
@.planning/phases/03-platform-readiness/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run local R CMD check before submitting to win-builder</name>
  <files></files>
  <action>
Run a final local check to ensure the package is clean before submitting to win-builder:

```r
# In R console
devtools::check(remote = TRUE, manual = TRUE)
```

This must pass with:
- 0 ERRORs
- 0 WARNINGs
- 0 NOTEs (or only the "New submission" NOTE which is expected)

If there are any ERRORs or WARNINGs, fix them before proceeding to win-builder.
  </action>
  <verify>
R CMD check output shows:
- Status: OK (or only "New submission" NOTE)
- 0 ERRORs
- 0 WARNINGs
  </verify>
  <done>Local R CMD check passes cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Submit to win-builder R-devel</name>
  <files></files>
  <action>
Submit package to win-builder R-devel:

```r
# In R console
devtools::check_win_devel()
```

This uploads the package to https://win-builder.r-project.org/ for testing with R-devel on Windows.

After submission, Claude should report:
- Submission successful
- Email will arrive in approximately 30 minutes
- User should check email for results link
  </action>
  <verify>
Console output confirms package was uploaded successfully to win-builder.
  </verify>
  <done>Package submitted to win-builder R-devel</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Submitted package to win-builder R-devel for Windows testing</what-built>
  <how-to-verify>
1. Check email inbox for win-builder results (arrives in ~30 minutes)
2. Open the results link in the email
3. Review the check log for:
   - 0 ERRORs (required)
   - 0 WARNINGs (required)
   - NOTEs (document any, "New submission" is expected)
4. Specifically verify:
   - hdf5r installed and linked correctly on Windows
   - mmap installed and linked correctly on Windows
   - All examples ran successfully
   - All tests passed

If there are ERRORs or WARNINGs:
- Report the specific error messages
- We will need to diagnose and fix before proceeding
  </how-to-verify>
  <resume-signal>Report: "R-devel passed" with any NOTEs, OR "R-devel failed" with error details</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Submit to win-builder R-release</name>
  <files></files>
  <action>
Submit package to win-builder R-release:

```r
# In R console
devtools::check_win_release()
```

This tests with the current stable R release on Windows.

After submission, Claude should report:
- Submission successful
- Email will arrive in approximately 30 minutes
- User should check email for results link
  </action>
  <verify>
Console output confirms package was uploaded successfully to win-builder.
  </verify>
  <done>Package submitted to win-builder R-release</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Submitted package to win-builder R-release for Windows testing</what-built>
  <how-to-verify>
1. Check email inbox for win-builder results (arrives in ~30 minutes)
2. Open the results link in the email
3. Review the check log for:
   - 0 ERRORs (required)
   - 0 WARNINGs (required)
   - NOTEs (document any)
4. Verify results are consistent with R-devel check

If there are ERRORs or WARNINGs:
- Report the specific error messages
- We will need to diagnose and fix before proceeding
  </how-to-verify>
  <resume-signal>Report: "R-release passed" with any NOTEs, OR "R-release failed" with error details</resume-signal>
</task>

</tasks>

<verification>
Phase 3 is complete when:
1. Local R CMD check passes with 0 errors, 0 warnings
2. win-builder R-devel check passes with 0 errors, 0 warnings
3. win-builder R-release check passes with 0 errors, 0 warnings
4. User confirms all three backends (hdf5, mem, mmap) work on Windows
</verification>

<success_criteria>
1. Both win-builder checks (R-devel and R-release) pass with 0 ERRORs, 0 WARNINGs
2. Any NOTEs are documented and explained (e.g., "New submission" is expected)
3. All examples and tests pass on Windows
4. Package is confirmed Windows-compatible and ready for CRAN submission
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-readiness/03-03-SUMMARY.md`
</output>
