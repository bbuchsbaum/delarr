---
phase: 03-platform-readiness
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - R/delarr-backends.R
  - R/delarr-writer-hdf5.R
  - man/delarr_hdf5.Rd
  - man/delarr_mmap.Rd
  - man/hdf5_writer.Rd
  - tests/testthat/test-core.R
autonomous: true

must_haves:
  truths:
    - "HDF5 and mmap examples run unconditionally (no if-requireNamespace wrapper)"
    - "Examples complete in under 5 seconds each"
    - "Test suite passes without requiring DELARR_SKIP_HDF5 escape hatch"
  artifacts:
    - path: "R/delarr-backends.R"
      provides: "Clean examples for delarr_hdf5() and delarr_mmap()"
    - path: "R/delarr-writer-hdf5.R"
      provides: "Clean examples for hdf5_writer()"
    - path: "tests/testthat/test-core.R"
      provides: "Simplified HDF5 test without defensive checks"
  key_links:
    - from: "roxygen2 @examples"
      to: "man/*.Rd"
      via: "devtools::document()"
      pattern: "@examples"
---

<objective>
Clean up examples and tests to remove conditional dependency checks that are now redundant.

Purpose: Since hdf5r and mmap are now required Imports, examples no longer need if-requireNamespace wrappers. Tests no longer need defensive checks or DELARR_SKIP_HDF5 escape hatch.

Output: Streamlined examples that run directly; simplified test code without conditional logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-readiness/03-CONTEXT.md
@.planning/phases/03-platform-readiness/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove conditional wrappers from HDF5 and mmap examples</name>
  <files>R/delarr-backends.R, R/delarr-writer-hdf5.R</files>
  <action>
Edit roxygen @examples in R/delarr-backends.R:

1. For `delarr_hdf5()` (around line 78-99), REMOVE the if-requireNamespace wrapper. Change from:
```r
#' @examples
#' if (requireNamespace("hdf5r", quietly = TRUE)) {
#'   # Create a temporary HDF5 file
#'   ...
#' }
```

To (direct example, no wrapper):
```r
#' @examples
#' # Create a temporary HDF5 file
#' tf <- tempfile(fileext = ".h5")
#' data <- matrix(1:20, nrow = 4, ncol = 5)
#'
#' # Write test data
#' f <- hdf5r::H5File$new(tf, mode = "w")
#' f$create_dataset("X", robj = data)
#' f$close_all()
#'
#' # Load as delayed array
#' darr <- delarr_hdf5(tf, "X")
#' darr
#'
#' # Apply operations and collect
#' result <- darr |> d_map(~ .x * 2) |> collect()
#' result
#'
#' # Clean up
#' unlink(tf)
```

2. For `delarr_mmap()` (around line 171-188), REMOVE the if-requireNamespace wrapper. Change to direct example:
```r
#' @examples
#' # Create a binary file with matrix data
#' mat <- matrix(1:20, nrow = 4, ncol = 5)
#' tf <- tempfile()
#' writeBin(as.double(mat), tf)
#'
#' # Load as delayed array
#' darr <- delarr_mmap(tf, nrow = 4, ncol = 5)
#' darr
#'
#' # Apply operations and collect
#' result <- darr |> d_map(~ .x * 2) |> collect()
#' result
#'
#' # Clean up
#' unlink(tf)
```

Edit roxygen @examples in R/delarr-writer-hdf5.R:

3. For `hdf5_writer()` (around line 20-45), REMOVE the if-requireNamespace wrapper. Change to direct example:
```r
#' @examples
#' # Create source data in a temp HDF5 file
#' tf_in <- tempfile(fileext = ".h5")
#' data <- matrix(1:20, nrow = 4, ncol = 5)
#' f <- hdf5r::H5File$new(tf_in, mode = "w")
#' f$create_dataset("X", robj = data)
#' f$close_all()
#'
#' # Load, transform, and stream to output file
#' darr <- delarr_hdf5(tf_in, "X")
#' transformed <- darr |> d_center(dim = "cols")
#'
#' tf_out <- tempfile(fileext = ".h5")
#' writer <- hdf5_writer(tf_out, "result", ncol = ncol(transformed), compression = 4L)
#' collect(transformed, into = writer)
#'
#' # Verify output
#' g <- hdf5r::H5File$new(tf_out, mode = "r")
#' result <- g[["result"]]$read()
#' g$close_all()
#' result
#'
#' # Clean up
#' unlink(c(tf_in, tf_out))
```

After editing, run `devtools::document()` to regenerate man/ files.
  </action>
  <verify>
1. `grep -n "if (requireNamespace" R/delarr-backends.R` returns NO matches
2. `grep -n "if (requireNamespace" R/delarr-writer-hdf5.R` returns NO matches
3. `grep -n "if (requireNamespace" man/delarr_hdf5.Rd` returns NO matches
4. `grep -n "if (requireNamespace" man/delarr_mmap.Rd` returns NO matches
5. `grep -n "if (requireNamespace" man/hdf5_writer.Rd` returns NO matches
6. `R CMD check --as-cran .` runs examples successfully
  </verify>
  <done>All HDF5 and mmap examples run directly without conditional wrappers</done>
</task>

<task type="auto">
  <name>Task 2: Simplify HDF5 test to remove defensive checks</name>
  <files>tests/testthat/test-core.R</files>
  <action>
Edit tests/testthat/test-core.R, in the test "HDF5 writer streams results to disk" (around line 236-264).

REMOVE the entire defensive check block at the start of the test:
```r
# HDF5 tests should FAIL when hdf5r unavailable - it's a required dependency for full test suite
# If you cannot install hdf5r, set environment variable DELARR_SKIP_HDF5=true to skip
if (Sys.getenv("DELARR_SKIP_HDF5") == "true") {
  skip("HDF5 tests skipped via DELARR_SKIP_HDF5 environment variable")
}
if (!requireNamespace("hdf5r", quietly = TRUE)) {
  stop("Package 'hdf5r' is required for full test suite. ",
       "Install it with: install.packages('hdf5r'). ",
       "Or set DELARR_SKIP_HDF5=true to skip HDF5 tests.", call. = FALSE)
}
```

The test should now start directly with:
```r
test_that("HDF5 writer streams results to disk", {
  path <- tempfile(fileext = ".h5")
  on.exit(unlink(path), add = TRUE)
  ...
})
```

Rationale: hdf5r is now a required Import. If it's not installed, the package won't load at all, so the test will never run anyway. No escape hatch needed.
  </action>
  <verify>
1. `grep -n "DELARR_SKIP_HDF5" tests/testthat/test-core.R` returns NO matches
2. `grep -n "requireNamespace.*hdf5r" tests/testthat/test-core.R` returns NO matches
3. `R -e "testthat::test_file('tests/testthat/test-core.R')"` passes all tests
  </verify>
  <done>HDF5 test simplified - no defensive checks, no escape hatch needed</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -r "if (requireNamespace" R/` returns NO matches (only function calls like `requireNamespace("matrixStats")` in utils.R remain)
2. `grep -r "if (requireNamespace" man/` returns NO matches
3. `grep "DELARR_SKIP_HDF5" tests/` returns NO matches
4. `devtools::document()` regenerates man/ files cleanly
5. `R CMD check --as-cran .` passes with 0 errors, 0 warnings
6. All examples run in under 5 seconds (small 4x5 or 5x6 matrices)
</verification>

<success_criteria>
1. No if-requireNamespace wrappers in HDF5 or mmap examples
2. No DELARR_SKIP_HDF5 escape hatch in tests
3. All examples run directly and complete in under 5 seconds
4. R CMD check passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-readiness/03-02-SUMMARY.md`
</output>
