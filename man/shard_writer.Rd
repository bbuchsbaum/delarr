% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-shard.R
\name{shard_writer}
\alias{shard_writer}
\title{Shared-memory writer for streaming \code{collect()}}
\usage{
shard_writer(nrow, ncol, backing = "auto")
}
\arguments{
\item{nrow, ncol}{Dimensions of the output matrix.}

\item{backing}{Backing type passed to \code{shard::buffer()}.}
}
\value{
A writer list with \verb{$write()}, \verb{$finalize()}, \verb{$result()}, and
\verb{$close()} methods.
}
\description{
Creates a writer object backed by \code{shard::buffer()} conforming to delarr's
writer protocol. Pass to \code{collect(x, into = shard_writer(...))} to stream
results into shared memory.
}
\examples{
if (requireNamespace("shard", quietly = TRUE)) {
  mat <- matrix(rnorm(20), 4, 5)
  darr <- delarr(mat)
  w <- shard_writer(4, 5)
  collect(darr |> d_map(~ .x * 2), into = w)
  w$result()
  w$close()
}
}
