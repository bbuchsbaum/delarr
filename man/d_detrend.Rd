% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-verbs.R
\name{d_detrend}
\alias{d_detrend}
\title{Detrend a delayed matrix}
\usage{
d_detrend(x, dim = c("rows", "cols"), axis = NULL, degree = 1L)
}
\arguments{
\item{x}{A \code{delarr}.}

\item{dim}{Dimension along which to fit the trend.}

\item{axis}{Integer axis for N-d arrays (alternative to \code{dim}).}

\item{degree}{Polynomial degree (default 1).}
}
\value{
A \code{delarr} with the detrend operation queued.
}
\description{
Removes a polynomial trend of the specified degree along the chosen
dimension.
}
\examples{
# Create matrix with linear trend in rows
mat <- matrix(1:12 + rep(1:4, each = 3), nrow = 3, ncol = 4)
darr <- delarr(mat)

# Remove linear trend along rows
detrended <- darr |> d_detrend(dim = "rows", degree = 1L) |> collect()
detrended

# Remove quadratic trend
quad_detrend <- darr |> d_detrend(dim = "rows", degree = 2L) |> collect()
quad_detrend
}
