---
phase: 02-code-quality
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - tests/testthat/test-core.R
autonomous: true

must_haves:
  truths:
    - "All tests pass on devtools::test()"
    - "R CMD check passes with 0 errors, 0 warnings"
    - "HDF5 tests run (not skip) when hdf5r available"
  artifacts:
    - path: "tests/testthat/test-core.R"
      provides: "Updated HDF5 test policy"
  key_links:
    - from: "tests/testthat/test-core.R"
      to: "hdf5r package"
      via: "requireNamespace check"
      pattern: "requireNamespace.*hdf5r"
---

<objective>
Final validation - ensure all tests pass, update HDF5 test policy per user requirement (FAIL not skip when hdf5r unavailable), and verify R CMD check passes.

Purpose: TEST-01 (tests pass) and TEST-06 (HDF5 test policy). This is the quality gate for Phase 2.
Output: Clean test suite with correct HDF5 dependency handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-code-quality/02-CONTEXT.md

@tests/testthat/test-core.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HDF5 test policy to FAIL when hdf5r unavailable</name>
  <files>tests/testthat/test-core.R</files>
  <action>
Per user requirement in 02-CONTEXT.md: "HDF5 tests should FAIL (not skip) when hdf5r unavailable - it's a real dependency for full suite"

Update the HDF5 test in test-core.R (line 236-255):

**Current code:**
```r
test_that("HDF5 writer streams results to disk", {
  skip_if_not_installed("hdf5r")
  # ... test code ...
})
```

**Updated code:**
```r
test_that("HDF5 writer streams results to disk", {
  # HDF5 tests should FAIL when hdf5r unavailable - it's a required dependency for full test suite
  # If you cannot install hdf5r, set environment variable DELARR_SKIP_HDF5=true to skip
  if (Sys.getenv("DELARR_SKIP_HDF5") == "true") {
    skip("HDF5 tests skipped via DELARR_SKIP_HDF5 environment variable")
  }
  if (!requireNamespace("hdf5r", quietly = TRUE)) {
    stop("Package 'hdf5r' is required for full test suite. ",
         "Install it with: install.packages('hdf5r'). ",
         "Or set DELARR_SKIP_HDF5=true to skip HDF5 tests.", call. = FALSE)
  }
  # ... rest of test code unchanged ...
})
```

This approach:
1. Fails tests when hdf5r missing (user requirement)
2. Provides escape hatch via environment variable for CI environments without HDF5
3. Gives clear error message with installation instructions
  </action>
  <verify>
```r
# With hdf5r installed - tests should run
devtools::test(filter = "core")

# Without hdf5r (simulated) - would fail with clear message
# (Can't easily test this without uninstalling hdf5r)
```
  </verify>
  <done>HDF5 test fails with clear error when hdf5r unavailable, provides DELARR_SKIP_HDF5 escape hatch</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify all tests pass</name>
  <files>tests/testthat/</files>
  <action>
Run the complete test suite to verify all tests pass:

```bash
cd /Users/bbuchsbaum/code/delarr
Rscript -e "devtools::test()"
```

Expected test files:
- test-core.R (existing, updated HDF5 policy)
- test-reductions.R (new from 02-01)
- test-edge-cases.R (new from 02-04)

If any tests fail:
1. Identify the failing test
2. Check if it's a real bug or test issue
3. Fix the test or underlying code
4. Re-run until all pass

Document test counts in summary.
  </action>
  <verify>
```bash
cd /Users/bbuchsbaum/code/delarr && Rscript -e "devtools::test()"
```
All tests pass (0 failures, 0 errors).
  </verify>
  <done>devtools::test() shows all tests passing</done>
</task>

<task type="auto">
  <name>Task 3: Run R CMD check and verify clean result</name>
  <files>None (verification only)</files>
  <action>
Run full R CMD check to ensure Phase 2 changes don't break anything:

```bash
cd /Users/bbuchsbaum/code/delarr
Rscript -e "devtools::check()"
```

Expected result: 0 errors, 0 warnings, 0 notes (or explained notes)

If issues arise:
1. Errors: Must fix before phase complete
2. Warnings: Must fix before phase complete
3. Notes: Document if acceptable (e.g., "New submission")

Common issues to watch for:
- Missing imports in NAMESPACE
- Documentation mismatches
- Test failures during check
- Examples that take too long
  </action>
  <verify>
```bash
cd /Users/bbuchsbaum/code/delarr && Rscript -e "devtools::check()"
```
Shows: 0 errors, 0 warnings.
  </verify>
  <done>R CMD check passes with 0 errors, 0 warnings</done>
</task>

</tasks>

<verification>
```bash
cd /Users/bbuchsbaum/code/delarr
Rscript -e "devtools::test()"
Rscript -e "devtools::check()"
```

Both commands complete successfully with no failures or errors.
</verification>

<success_criteria>
1. HDF5 tests fail (not skip) when hdf5r unavailable, with clear error message
2. DELARR_SKIP_HDF5 environment variable provides escape hatch
3. All tests pass on devtools::test()
4. R CMD check passes with 0 errors, 0 warnings
5. Phase 2 requirements (CODE-01 through CODE-04, TEST-01 through TEST-06) are all addressed
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-quality/02-05-SUMMARY.md`

Include:
- Total test count across all test files
- R CMD check result summary
- Confirmation that all 10 Phase 2 requirements are met
</output>
