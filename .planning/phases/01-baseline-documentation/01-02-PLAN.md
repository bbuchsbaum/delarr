---
phase: 01-baseline-documentation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/delarr-core.R
  - R/delarr-eval.R
  - R/delarr-verbs.R
autonomous: true

must_haves:
  truths:
    - "delarr() has runnable @examples showing matrix wrapping"
    - "collect() has runnable @examples showing materialization"
    - "d_map() has runnable @examples showing elementwise transformation"
    - "d_map2() has runnable @examples showing binary operations"
    - "d_reduce() has runnable @examples showing row/column reduction"
    - "block_apply() has runnable @examples showing chunked processing"
  artifacts:
    - path: "R/delarr-core.R"
      provides: "Core function documentation with examples"
      contains: "@examples"
    - path: "R/delarr-eval.R"
      provides: "Evaluation function documentation with examples"
      contains: "@examples"
    - path: "R/delarr-verbs.R"
      provides: "d_map, d_map2, d_reduce documentation with examples"
      contains: "@examples"
  key_links:
    - from: "delarr() example"
      to: "collect()"
      via: "pipeline demonstration"
      pattern: "collect\\("
---

<objective>
Add @examples to core delarr functions: delarr(), collect(), d_map(), d_map2(), d_reduce(), and block_apply().

Purpose: These 6 functions form the core API and must have complete documentation examples for CRAN submission (DOCS-03).
Output: All 6 core functions have runnable @examples that demonstrate their primary use cases.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-RESEARCH.md
@R/delarr-core.R
@R/delarr-eval.R
@R/delarr-verbs.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @examples to delarr() and collect()</name>
  <files>R/delarr-core.R, R/delarr-eval.R</files>
  <action>
Add @examples to the delarr() function in R/delarr-core.R:

```r
#' @examples
#' # Create a delayed matrix from a regular matrix
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#' darr
#'
#' # Operations are queued lazily
#' result <- darr * 2
#' result
#'
#' # Materialize with collect()
#' collect(result)
```

Add @examples to the collect() function in R/delarr-eval.R:

```r
#' @examples
#' # Basic materialization
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#' collect(darr)
#'
#' # Collect after lazy operations
#' result <- darr |>
#'   d_map(~ .x^2) |>
#'   collect()
#' result
```

Keep examples small (3x4 matrices) to ensure <5 second runtime.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("delarr", package = "delarr")
example("collect", package = "delarr")
```
Both should complete without error in <5 seconds.
  </verify>
  <done>delarr() and collect() have @examples that run without error.</done>
</task>

<task type="auto">
  <name>Task 2: Add @examples to d_map(), d_map2(), d_reduce()</name>
  <files>R/delarr-verbs.R</files>
  <action>
Add @examples to d_map() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#'
#' # Apply elementwise transformation with formula
#' squared <- darr |> d_map(~ .x^2) |> collect()
#' squared
#'
#' # Apply with function
#' logged <- darr |> d_map(log1p) |> collect()
#' logged
```

Add @examples to d_map2() in R/delarr-verbs.R:

```r
#' @examples
#' mat1 <- matrix(1:12, nrow = 3, ncol = 4)
#' mat2 <- matrix(12:1, nrow = 3, ncol = 4)
#' darr1 <- delarr(mat1)
#' darr2 <- delarr(mat2)
#'
#' # Binary operation between two delayed matrices
#' added <- d_map2(darr1, darr2, ~ .x + .y) |> collect()
#' added
#'
#' # Binary operation with scalar
#' scaled <- d_map2(darr1, 10, ~ .x * .y) |> collect()
#' scaled
```

Add @examples to d_reduce() in R/delarr-verbs.R:

```r
#' @examples
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr(mat)
#'
#' # Row sums (reduce across columns for each row)
#' row_sums <- darr |> d_reduce(sum, dim = "rows") |> collect()
#' row_sums
#'
#' # Column means (reduce across rows for each column)
#' col_means <- darr |> d_reduce(mean, dim = "cols") |> collect()
#' col_means
```

All examples use small matrices (3x4) to ensure fast execution.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("d_map", package = "delarr")
example("d_map2", package = "delarr")
example("d_reduce", package = "delarr")
```
All should complete without error in <5 seconds.
  </verify>
  <done>d_map(), d_map2(), and d_reduce() have @examples that run without error.</done>
</task>

<task type="auto">
  <name>Task 3: Add @examples to block_apply()</name>
  <files>R/delarr-eval.R</files>
  <action>
Add @examples to block_apply() in R/delarr-eval.R:

```r
#' @examples
#' mat <- matrix(1:20, nrow = 4, ncol = 5)
#' darr <- delarr(mat)
#'
#' # Apply function to column chunks
#' col_maxes <- block_apply(darr, margin = "cols", size = 2L, fn = function(block) {
#'   apply(block, 2, max)
#' })
#' unlist(col_maxes)
#'
#' # Apply function to row chunks
#' row_means <- block_apply(darr, margin = "rows", size = 2L, fn = function(block) {
#'   rowMeans(block)
#' })
#' unlist(row_means)
```

Use small chunk sizes (2) with small matrices (4x5) to demonstrate chunking behavior while keeping runtime minimal.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("block_apply", package = "delarr")
```
Should complete without error in <5 seconds.
  </verify>
  <done>block_apply() has @examples that run without error.</done>
</task>

</tasks>

<verification>
- Run `devtools::document()` to regenerate .Rd files
- Run `devtools::check()` and verify no documentation warnings
- All 6 examples run without error: delarr(), collect(), d_map(), d_map2(), d_reduce(), block_apply()
- Each example completes in <5 seconds
</verification>

<success_criteria>
1. All 6 functions have @examples sections in their roxygen2 comments
2. `devtools::run_examples()` completes without error
3. Each example demonstrates the primary use case of its function
4. No files written outside tempdir() (none needed for these in-memory examples)
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-02-SUMMARY.md`
</output>
