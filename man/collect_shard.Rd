% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-shard.R
\name{collect_shard}
\alias{collect_shard}
\title{Parallel collect using shard's shared-memory workers}
\usage{
collect_shard(x, workers = NULL, chunk_size = NULL, optimize = TRUE)
}
\arguments{
\item{x}{A \code{delarr} object.}

\item{workers}{Number of worker processes. Defaults to
\code{parallel::detectCores() - 1}.}

\item{chunk_size}{Column chunk size for sharding. If \code{NULL}, uses the
seed's \code{chunk_hint} or a sensible default.}

\item{optimize}{Logical; run DAG optimizations before evaluation.}
}
\value{
A materialised matrix or vector.
}
\description{
Evaluates a \code{delarr} pipeline in parallel using \code{shard::shard_map()}. This
gives proper multi-process parallelism with shared-memory I/O, including
parallel reductions.
}
\details{
Pipelines that require full-matrix evaluation (row-wise center/scale/zscore/
detrend), paired RHS delarrs (\code{d_map2} with two delarrs), or generic
(user-supplied) reductions automatically fall back to sequential \code{collect()}.
}
\examples{
if (requireNamespace("shard", quietly = TRUE)) {
  mat <- matrix(rnorm(100), 10, 10)
  darr <- delarr_shard(mat)
  result <- collect_shard(darr |> d_map(~ .x^2), workers = 2)
  all.equal(result, mat^2)
}
}
