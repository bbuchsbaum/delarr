# Transpose a delayed matrix

Transpose a delayed matrix

## Usage

``` r
d_transpose(x, chunk_size = NULL)
```

## Arguments

- x:

  A `delarr`.

- chunk_size:

  Optional chunk size used for internal pulls.

## Value

A transposed `delarr`.
