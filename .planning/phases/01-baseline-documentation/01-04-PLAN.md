---
phase: 01-baseline-documentation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - R/delarr-backends.R
  - R/delarr-writer-hdf5.R
autonomous: true

must_haves:
  truths:
    - "delarr_backend() has runnable @examples showing custom backend creation"
    - "delarr_mem() has runnable @examples showing in-memory wrapper"
    - "delarr_hdf5() has runnable @examples wrapped in requireNamespace() check"
    - "hdf5_writer() has runnable @examples wrapped in requireNamespace() check"
    - "delarr_mmap() has @examples showing current stub behavior"
    - "All HDF5 examples use tempfile() for file I/O"
  artifacts:
    - path: "R/delarr-backends.R"
      provides: "Backend function documentation with examples"
      contains: "@examples"
    - path: "R/delarr-writer-hdf5.R"
      provides: "HDF5 writer documentation with conditional examples"
      contains: "requireNamespace"
  key_links:
    - from: "delarr_hdf5() example"
      to: "tempfile()"
      via: "file path generation"
      pattern: "tempfile\\("
    - from: "hdf5_writer() example"
      to: "tempfile()"
      via: "file path generation"
      pattern: "tempfile\\("
---

<objective>
Add @examples to HDF5 and backend functions: delarr_backend(), delarr_mem(), delarr_hdf5(), hdf5_writer(), and delarr_mmap().

Purpose: Complete documentation for the 5 backend/storage functions (DOCS-03). HDF5 examples must be conditional and use tempfile() (DOCS-04, DOCS-05).
Output: All 5 functions have @examples. HDF5 examples are wrapped in requireNamespace() checks and use tempfile() for all file I/O.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-RESEARCH.md
@R/delarr-backends.R
@R/delarr-writer-hdf5.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @examples to delarr_backend() and delarr_mem()</name>
  <files>R/delarr-backends.R</files>
  <action>
Add @examples to delarr_backend() in R/delarr-backends.R:

```r
#' @examples
#' # Create a custom backend from a pull function
#' data <- matrix(1:20, nrow = 4, ncol = 5)
#'
#' darr <- delarr_backend(
#'   nrow = 4,
#'   ncol = 5,
#'   pull = function(rows = NULL, cols = NULL) {
#'     rows <- rows %||% seq_len(4)
#'     cols <- cols %||% seq_len(5)
#'     data[rows, cols, drop = FALSE]
#'   }
#' )
#' darr
#'
#' # Use like any delarr
#' result <- darr |> d_map(~ .x * 2) |> collect()
#' result
```

Note: Use `%||%` from rlang which is already imported.

Add @examples to delarr_mem() in R/delarr-backends.R:

```r
#' @examples
#' # Wrap an in-memory matrix
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr_mem(mat)
#' darr
#'
#' # Apply operations lazily
#' result <- darr |> d_center(dim = "rows") |> collect()
#' result
```

These are simple in-memory examples with no file I/O concerns.
  </action>
  <verify>
Run in R:
```r
devtools::load_all()
example("delarr_backend", package = "delarr")
example("delarr_mem", package = "delarr")
```
Both should complete without error in <5 seconds.
  </verify>
  <done>delarr_backend() and delarr_mem() have @examples that run without error.</done>
</task>

<task type="auto">
  <name>Task 2: Add @examples to delarr_hdf5() with conditional execution</name>
  <files>R/delarr-backends.R</files>
  <action>
Add @examples to delarr_hdf5() in R/delarr-backends.R. The example MUST:
1. Be wrapped in `if (requireNamespace("hdf5r", quietly = TRUE))`
2. Use `tempfile(fileext = ".h5")` for all file paths
3. Clean up with `unlink()` at the end

```r
#' @examples
#' if (requireNamespace("hdf5r", quietly = TRUE)) {
#'   # Create a temporary HDF5 file
#'   tf <- tempfile(fileext = ".h5")
#'   data <- matrix(1:20, nrow = 4, ncol = 5)
#'
#'   # Write test data
#'   f <- hdf5r::H5File$new(tf, mode = "w")
#'   f$create_dataset("X", robj = data)
#'   f$close_all()
#'
#'   # Load as delayed array
#'   darr <- delarr_hdf5(tf, "X")
#'   darr
#'
#'   # Apply operations and collect
#'   result <- darr |> d_map(~ .x * 2) |> collect()
#'   result
#'
#'   # Clean up
#'   unlink(tf)
#' }
```

CRITICAL: File paths MUST use tempfile(). CRAN auto-rejects packages that write to working directory.
  </action>
  <verify>
Run in R (assuming hdf5r is installed):
```r
devtools::load_all()
example("delarr_hdf5", package = "delarr")
# Verify no files left behind
list.files(pattern = "\\.h5$")  # Should be empty
```
Example should complete without error in <5 seconds.
  </verify>
  <done>delarr_hdf5() has conditional @examples using tempfile() and unlink() cleanup.</done>
</task>

<task type="auto">
  <name>Task 3: Add @examples to hdf5_writer() and delarr_mmap()</name>
  <files>R/delarr-writer-hdf5.R, R/delarr-backends.R</files>
  <action>
Add @examples to hdf5_writer() in R/delarr-writer-hdf5.R. The example MUST:
1. Be wrapped in `if (requireNamespace("hdf5r", quietly = TRUE))`
2. Use `tempfile(fileext = ".h5")` for all file paths
3. Clean up with `unlink()` at the end

```r
#' @examples
#' if (requireNamespace("hdf5r", quietly = TRUE)) {
#'   # Create source data in a temp HDF5 file
#'   tf_in <- tempfile(fileext = ".h5")
#'   data <- matrix(1:20, nrow = 4, ncol = 5)
#'   f <- hdf5r::H5File$new(tf_in, mode = "w")
#'   f$create_dataset("X", robj = data)
#'   f$close_all()
#'
#'   # Load, transform, and stream to output file
#'   darr <- delarr_hdf5(tf_in, "X")
#'   transformed <- darr |> d_center(dim = "cols")
#'
#'   tf_out <- tempfile(fileext = ".h5")
#'   writer <- hdf5_writer(tf_out, "result", ncol = ncol(transformed))
#'   collect(transformed, into = writer)
#'
#'   # Verify output
#'   g <- hdf5r::H5File$new(tf_out, mode = "r")
#'   result <- g[["result"]]$read()
#'   g$close_all()
#'   result
#'
#'   # Clean up
#'   unlink(c(tf_in, tf_out))
#' }
```

Add @examples to delarr_mmap() in R/delarr-backends.R showing the stub behavior:

```r
#' @examples
#' # delarr_mmap() is not yet implemented
#' \dontrun{
#' delarr_mmap()  # Throws an error
#' }
#'
#' # Use delarr_backend() with a custom pull function instead
#' mat <- matrix(1:12, nrow = 3, ncol = 4)
#' darr <- delarr_backend(
#'   nrow = 3, ncol = 4,
#'   pull = function(rows = NULL, cols = NULL) {
#'     rows <- rows %||% seq_len(3)
#'     cols <- cols %||% seq_len(4)
#'     mat[rows, cols, drop = FALSE]
#'   }
#' )
#' collect(darr)
```

The delarr_mmap() example uses \dontrun{} for the error case (acceptable) but shows the workaround that does run.
  </action>
  <verify>
Run in R (assuming hdf5r is installed):
```r
devtools::load_all()
example("hdf5_writer", package = "delarr")
example("delarr_mmap", package = "delarr")
# Verify no files left behind
list.files(pattern = "\\.h5$")  # Should be empty
```
Both should complete without error in <5 seconds.
  </verify>
  <done>hdf5_writer() has conditional @examples using tempfile(), delarr_mmap() documents stub status with workaround.</done>
</task>

</tasks>

<verification>
- Run `devtools::document()` to regenerate .Rd files
- Run `devtools::check()` and verify no documentation warnings
- All 5 examples run without error: delarr_backend(), delarr_mem(), delarr_hdf5(), hdf5_writer(), delarr_mmap()
- HDF5 examples are conditional on hdf5r availability
- All file I/O uses tempfile() with cleanup via unlink()
- No .h5 files left in working directory after examples run
</verification>

<success_criteria>
1. All 5 functions have @examples sections
2. delarr_hdf5() and hdf5_writer() examples wrapped in `if (requireNamespace("hdf5r", ...))`
3. All HDF5 file paths use `tempfile(fileext = ".h5")`
4. All HDF5 examples call `unlink()` to clean up temp files
5. `devtools::run_examples()` completes without error
6. No files written to working directory (DOCS-05)
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-04-SUMMARY.md`
</output>
