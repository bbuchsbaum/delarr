% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-eval.R
\name{block_apply}
\alias{block_apply}
\title{Apply a function to streamed matrix blocks}
\usage{
block_apply(x, margin = c("cols", "rows"), size = 16384L, fn)
}
\arguments{
\item{x}{A \code{delarr} object.}

\item{margin}{Dimension along which to chunk (\code{"cols"} or \code{"rows"}).}

\item{size}{Approximate chunk size.}

\item{fn}{Function applied to each materialised chunk.}
}
\value{
A list of results returned by \code{fn}.
}
\description{
Evaluates a \code{delarr} slice-by-slice, materialising manageable chunks for
further processing without realising the full matrix.
}
\examples{
mat <- matrix(1:20, nrow = 4, ncol = 5)
darr <- delarr(mat)

# Apply function to column chunks
col_maxes <- block_apply(darr, margin = "cols", size = 2L, fn = function(block) {
  apply(block, 2, max)
})
unlist(col_maxes)

# Apply function to row chunks
row_means <- block_apply(darr, margin = "rows", size = 2L, fn = function(block) {
  rowMeans(block)
})
unlist(row_means)

}
