---
phase: 01-baseline-documentation
plan: 05
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-04"]
files_modified:
  - inst/WORDLIST
autonomous: true

must_haves:
  truths:
    - "spelling::spell_check_package() passes with 0 typos"
    - "R CMD check --as-cran passes with 0 errors, 0 warnings"
    - "All examples run successfully in <5 seconds each"
    - "Vignette builds without errors"
  artifacts:
    - path: "inst/WORDLIST"
      provides: "Package-specific spelling exceptions"
  key_links:
    - from: "devtools::check()"
      to: "all documentation examples"
      via: "example execution"
      pattern: "examples"
---

<objective>
Run spelling check, create WORDLIST if needed, and verify final R CMD check passes with 0 errors, 0 warnings, 0 NOTEs.

Purpose: Complete Phase 1 validation (DOCS-07, CHECK-01, CHECK-02, CHECK-03, CHECK-04).
Output: Clean R CMD check with all documentation complete and spelling verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run spelling check and create WORDLIST</name>
  <files>inst/WORDLIST</files>
  <action>
Run spelling check and handle any flagged words:

```r
# Install spelling if needed
if (!requireNamespace("spelling", quietly = TRUE)) {
  install.packages("spelling")
}

# Run spell check
results <- spelling::spell_check_package()
print(results)
```

If legitimate technical terms are flagged (e.g., "delarr", "HDF5", "chunked", "matrixStats", "roxygen2", "elementwise"):
1. Create `inst/WORDLIST` file
2. Add one word per line (case-sensitive)
3. Re-run spell check to verify

Expected legitimate words to add to WORDLIST:
- delarr (package name)
- HDF5 (file format)
- hdf5r (package name)
- matrixStats (package name)
- elementwise
- chunked
- DAG
- fused
- mmap
- tempdir
- tempfile
- roxygen
- testthat
- rmarkdown
- zscore

Create inst directory if it doesn't exist:
```bash
mkdir -p inst
```
  </action>
  <verify>
Run in R:
```r
results <- spelling::spell_check_package()
nrow(results) == 0  # Should be TRUE after WORDLIST is complete
```
  </verify>
  <done>spelling::spell_check_package() returns 0 typos with valid WORDLIST.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate documentation and verify examples</name>
  <files></files>
  <action>
Regenerate all documentation and run examples:

```r
# Regenerate .Rd files from roxygen2 comments
devtools::document()

# Run all examples to verify they work
devtools::run_examples(run_donttest = FALSE, run_dontrun = FALSE)
```

If any example fails:
1. Note which function failed
2. Check the error message
3. Fix the @examples in the source file
4. Re-run devtools::document() and devtools::run_examples()

Verify example timing - each should complete in <5 seconds. If any example is slow:
1. Reduce matrix sizes
2. Use simpler operations
3. Add `\donttest{}` wrapper only if truly necessary (not preferred)
  </action>
  <verify>
```r
# This should complete without error
devtools::run_examples(run_donttest = FALSE, run_dontrun = FALSE)
```
All examples run successfully.
  </verify>
  <done>All documentation examples run without error in <5 seconds each.</done>
</task>

<task type="auto">
  <name>Task 3: Final R CMD check validation</name>
  <files></files>
  <action>
Run full R CMD check to verify Phase 1 completion:

```bash
cd /Users/bbuchsbaum/code/delarr
R CMD build . --no-manual
R CMD check $(ls -t *.tar.gz | head -1) --as-cran 2>&1 | tee check_results.txt
```

Or in R:
```r
devtools::check()
```

Expected result: 0 errors, 0 warnings, 0 NOTEs

If any issues remain:
1. Document the specific error/warning/note
2. Determine if it's a Phase 1 issue (docs, LICENSE, .Rbuildignore) or deferred issue (Phase 2/3)
3. Fix Phase 1 issues; note deferred issues for later phases

Verify URL check passes (DOCS-08):
```r
urlchecker::url_check()  # Already confirmed passing in research
```

Verify vignette builds (DOCS-06):
```r
devtools::build_vignettes()
```
  </action>
  <verify>
R CMD check output shows:
- 0 errors
- 0 warnings
- 0 NOTEs (or only acceptable NOTEs unrelated to Phase 1 requirements)

```bash
grep -E "^(ERROR|WARNING|NOTE)" check_results.txt
# Should show nothing or only explained notes
```
  </verify>
  <done>R CMD check --as-cran passes with 0 errors, 0 warnings, 0 NOTEs (or documented acceptable NOTEs).</done>
</task>

</tasks>

<verification>
- spelling::spell_check_package() returns 0 issues
- urlchecker::url_check() returns "All URLs are correct!"
- devtools::build_vignettes() completes without error
- devtools::run_examples() completes without error
- devtools::check() or R CMD check --as-cran passes cleanly
</verification>

<success_criteria>
1. **CHECK-01:** R CMD check passes with 0 errors
2. **CHECK-02:** R CMD check passes with 0 warnings
3. **CHECK-03:** R CMD check passes with 0 NOTEs (or documented acceptable NOTEs)
4. **CHECK-04:** All examples complete in <5 seconds each
5. **DOCS-01:** All @param documented (verified by check)
6. **DOCS-02:** All @return documented (verified by check)
7. **DOCS-03:** All exported functions have @examples (16/16)
8. **DOCS-04:** No \dontrun{} shortcuts except for stub functions
9. **DOCS-05:** All file I/O uses tempdir()/tempfile()
10. **DOCS-06:** Vignette builds without errors
11. **DOCS-07:** Spelling passes (spelling::spell_check_package())
12. **DOCS-08:** All URLs valid (urlchecker::url_check())
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-05-SUMMARY.md`
</output>
