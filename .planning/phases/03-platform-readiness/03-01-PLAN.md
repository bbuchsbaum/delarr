---
phase: 03-platform-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - R/delarr-backends.R
  - R/delarr-writer-hdf5.R
autonomous: true

must_haves:
  truths:
    - "hdf5r and mmap are listed in Imports field of DESCRIPTION"
    - "Package installs successfully with hdf5r and mmap as required dependencies"
    - "delarr_hdf5(), delarr_mmap(), and hdf5_writer() work without defensive checks"
  artifacts:
    - path: "DESCRIPTION"
      provides: "Package metadata with hdf5r and mmap in Imports"
      contains: "Imports:"
    - path: "R/delarr-backends.R"
      provides: "Backend functions without requireNamespace checks"
    - path: "R/delarr-writer-hdf5.R"
      provides: "HDF5 writer without requireNamespace check"
  key_links:
    - from: "DESCRIPTION Imports"
      to: "R code using hdf5r::"
      via: "R namespace mechanism"
      pattern: "hdf5r::"
    - from: "DESCRIPTION Imports"
      to: "R code using mmap::"
      via: "R namespace mechanism"
      pattern: "mmap::"
---

<objective>
Move hdf5r and mmap from Suggests to Imports and remove defensive requireNamespace() checks from code.

Purpose: Since user decided hdf5r and mmap are required dependencies, the defensive code patterns are now redundant. Imports guarantees the packages are available at runtime.

Output: DESCRIPTION updated with hdf5r/mmap in Imports; R code cleaned of unnecessary requireNamespace() checks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-platform-readiness/03-CONTEXT.md
@.planning/phases/03-platform-readiness/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DESCRIPTION to move hdf5r and mmap to Imports</name>
  <files>DESCRIPTION</files>
  <action>
Edit DESCRIPTION to:
1. Move `hdf5r` from Suggests to Imports
2. Move `mmap` from Suggests to Imports
3. Keep matrixStats, testthat, knitr, rmarkdown in Suggests (they remain optional)

The Imports section should become:
```
Imports:
    rlang,
    hdf5r,
    mmap
```

The Suggests section should become:
```
Suggests:
    matrixStats,
    testthat (>= 3.1.0),
    knitr,
    rmarkdown
```

Note: Order within sections is not critical but alphabetical is conventional.
  </action>
  <verify>
1. `grep -A5 "^Imports:" DESCRIPTION` shows rlang, hdf5r, mmap
2. `grep -A5 "^Suggests:" DESCRIPTION` shows matrixStats, testthat, knitr, rmarkdown (NOT hdf5r, NOT mmap)
3. `R CMD check --as-cran .` completes without errors about missing dependencies
  </verify>
  <done>DESCRIPTION has hdf5r and mmap in Imports field, not in Suggests</done>
</task>

<task type="auto">
  <name>Task 2: Remove requireNamespace checks for hdf5r and mmap from R code</name>
  <files>R/delarr-backends.R, R/delarr-writer-hdf5.R</files>
  <action>
Edit R/delarr-backends.R to remove the defensive checks:

1. In `delarr_hdf5()` (around line 101), REMOVE:
```r
if (!requireNamespace("hdf5r", quietly = TRUE)) {
  stop("Package 'hdf5r' is required for delarr_hdf5", call. = FALSE)
}
```

2. In `delarr_mmap()` (around line 190), REMOVE:
```r
if (!requireNamespace("mmap", quietly = TRUE)) {
  stop("Package 'mmap' is required for delarr_mmap(). ",
       "Install it or use delarr_backend() with a custom pull function.",
       call. = FALSE)
}
```

Edit R/delarr-writer-hdf5.R to remove the defensive check:

3. In `hdf5_writer()` (around line 47), REMOVE:
```r
if (!requireNamespace("hdf5r", quietly = TRUE)) {
  stop("Package 'hdf5r' is required for hdf5_writer()", call. = FALSE)
}
```

Keep all other code exactly as-is. The functions should just use hdf5r:: and mmap:: directly since they're now guaranteed present via Imports.

Important: Do NOT remove the requireNamespace() checks in R/utils.R for matrixStats - that's a different package that IS optional (Suggests).
  </action>
  <verify>
1. `grep -n "requireNamespace.*hdf5r" R/` returns NO matches in delarr-backends.R or delarr-writer-hdf5.R
2. `grep -n "requireNamespace.*mmap" R/` returns NO matches
3. `grep -n "requireNamespace.*matrixStats" R/utils.R` STILL returns matches (matrixStats check preserved)
4. `R -e "library(delarr); delarr_hdf5"` shows function exists without error
5. `R CMD check --as-cran .` passes
  </verify>
  <done>All requireNamespace() checks for hdf5r and mmap removed from R code; matrixStats check preserved</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep "^Imports:" -A5 DESCRIPTION` shows hdf5r, mmap, rlang
2. `grep "^Suggests:" -A5 DESCRIPTION` does NOT show hdf5r or mmap
3. `grep -r "requireNamespace.*hdf5r" R/` returns NO matches
4. `grep -r "requireNamespace.*mmap" R/` returns NO matches
5. `grep -r "requireNamespace.*matrixStats" R/` returns matches (preserved)
6. `R CMD check --as-cran .` passes with 0 errors, 0 warnings
</verification>

<success_criteria>
1. hdf5r and mmap are in DESCRIPTION Imports field
2. No requireNamespace() checks for hdf5r or mmap in R/ directory
3. matrixStats requireNamespace() check preserved in R/utils.R
4. Package builds and checks cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-platform-readiness/03-01-SUMMARY.md`
</output>
