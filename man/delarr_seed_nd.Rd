% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-seed.R
\name{delarr_seed_nd}
\alias{delarr_seed_nd}
\title{Construct an N-dimensional seed backend for \code{delarr}}
\usage{
delarr_seed_nd(
  dims,
  pull,
  chunk_hint = NULL,
  dimnames = NULL,
  begin = NULL,
  end = NULL
)
}
\arguments{
\item{dims}{Integer vector of dimension extents (length >= 2).}

\item{pull}{A function accepting a single argument \code{indices} — a named or
positional list of integer vectors (one per dimension, or \code{NULL} for "all")
— and returning an array with the requested sub-dimensions.}

\item{chunk_hint}{Optional list describing preferred chunk sizes.}

\item{dimnames}{Optional list of dimnames (one element per dimension).}

\item{begin}{Optional function invoked before streaming begins.}

\item{end}{Optional function invoked after streaming completes.}
}
\value{
An object of class \code{delarr_seed}.
}
\description{
Creates a seed for arrays with 2 or more dimensions. The pull function
receives a list of per-dimension index vectors and returns the
corresponding sub-array.
}
\examples{
arr <- array(seq_len(24), dim = c(3, 4, 2))
seed <- delarr_seed_nd(
  dims = c(3, 4, 2),
  pull = function(indices) {
    idx <- lapply(seq_along(dim(arr)), function(k) indices[[k]] \%||\% seq_len(dim(arr)[k]))
    do.call("[", c(list(arr), idx, list(drop = FALSE)))
  }
)
dim(seed)
}
