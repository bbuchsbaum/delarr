% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delarr-core.R
\name{Math.delarr}
\alias{Math.delarr}
\title{Elementwise math functions for \code{delarr}}
\usage{
\method{Math}{delarr}(x, ...)
}
\arguments{
\item{x}{A \code{delarr}.}

\item{...}{Additional arguments forwarded to the math generic.}
}
\value{
A \code{delarr} representing the fused elementwise operation.
}
\description{
Supports the R \code{Math} group generics (\code{sqrt()}, \code{abs()},
\code{exp()}, \code{log()}, \code{round()}, \code{floor()}, the trig
functions, and so on) lazily, as fused elementwise maps. Extra arguments are
forwarded to the underlying function, so \code{round(x, 2)} and
\code{log(x, base = 2)} work as expected.
}
\details{
The cumulative generics (\code{cumsum()}, \code{cumprod()}, \code{cummax()},
\code{cummin()}) are not elementwise and cannot be evaluated chunk-by-chunk,
so they raise an error rather than returning silently incorrect results.
}
\examples{
mat <- matrix(1:12, nrow = 3, ncol = 4)
darr <- delarr(mat)
collect(sqrt(darr))
collect(-darr)
}
