---
phase: 01-baseline-documentation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - R/delarr-seed.R
  - man/delarr_seed.Rd
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "delarr_seed() has runnable @examples in roxygen2 documentation"
    - "man/delarr_seed.Rd contains \\examples section after devtools::document()"
    - "Example runs successfully in <5 seconds"
  artifacts:
    - path: "R/delarr-seed.R"
      provides: "@examples section for delarr_seed()"
      contains: "@examples"
    - path: "man/delarr_seed.Rd"
      provides: "Generated .Rd file with examples"
      contains: "\\examples"
  key_links:
    - from: "R/delarr-seed.R"
      to: "man/delarr_seed.Rd"
      via: "devtools::document() roxygen2 processing"
      pattern: "@examples"
---

<objective>
Add @examples section to delarr_seed() to close the DOCS-03 gap.

Purpose: delarr_seed() is an exported function that lacks @examples documentation. This violates the DOCS-03 requirement that "Every exported function has runnable @examples." While users typically use the higher-level delarr_backend() wrapper, delarr_seed() is part of the public API and should be documented completely.

Output: R/delarr-seed.R updated with @examples, man/delarr_seed.Rd regenerated with \examples section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-VERIFICATION.md

# Reference: delarr_backend() @examples pattern (lines 15-32)
@R/delarr-backends.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @examples to delarr_seed()</name>
  <files>R/delarr-seed.R</files>
  <action>
Add an @examples section to the delarr_seed() function documentation in R/delarr-seed.R.

Insert the @examples block AFTER the existing @return line (line 15) and BEFORE the @export line (line 16).

The example should:
1. Create a small 3x4 matrix as the backing data
2. Define a pull function that accepts rows and cols parameters
3. Call delarr_seed() with nrow, ncol, and the pull function
4. Show how to wrap the seed in delarr() and use it
5. Use `%||%` for null-coalescing (already used in package)

Pattern to follow (similar to delarr_backend() but at seed level):

```r
#' @examples
#' # Create a custom seed with a pull function
#' data <- matrix(1:12, nrow = 3, ncol = 4)
#'
#' seed <- delarr_seed(
#'   nrow = 3,
#'   ncol = 4,
#'   pull = function(rows = NULL, cols = NULL) {
#'     rows <- rows %||% seq_len(3)
#'     cols <- cols %||% seq_len(4)
#'     data[rows, cols, drop = FALSE]
#'   }
#' )
#' seed
#'
#' # Wrap in delarr() to use with lazy operations
#' darr <- delarr(seed)
#' result <- darr |> d_map(~ .x * 2) |> collect()
#' result
```

Keep the example small (3x4 matrix) to ensure fast execution (<5 seconds).
  </action>
  <verify>
Run in R:
```r
grep("@examples", readLines("R/delarr-seed.R"), value = TRUE)
```
Should return a line containing "@examples".
  </verify>
  <done>R/delarr-seed.R contains @examples section with working example code</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate documentation and verify</name>
  <files>man/delarr_seed.Rd</files>
  <action>
Run devtools::document() to regenerate the man/delarr_seed.Rd file from the updated roxygen2 comments.

Then verify the example runs successfully:
1. Run devtools::document() to regenerate .Rd files
2. Run devtools::run_examples("delarr_seed") to verify the example executes
3. Check that man/delarr_seed.Rd now contains \examples section

Commands:
```r
devtools::document()
devtools::run_examples("delarr_seed")
```

The example should complete in well under 5 seconds (target: <1 second for a 3x4 matrix).
  </action>
  <verify>
Run in R:
```r
# Check .Rd file has examples
any(grepl("\\\\examples", readLines("man/delarr_seed.Rd")))
# Should return TRUE

# Run the example
devtools::run_examples("delarr_seed")
# Should complete without error
```
  </verify>
  <done>man/delarr_seed.Rd contains \examples section, example runs successfully</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Documentation check:**
   ```r
   # Verify @examples in source
   any(grepl("@examples", readLines("R/delarr-seed.R")))
   # TRUE

   # Verify \examples in generated .Rd
   any(grepl("\\\\examples", readLines("man/delarr_seed.Rd")))
   # TRUE
   ```

2. **Example execution:**
   ```r
   devtools::run_examples("delarr_seed")
   # Completes without error
   ```

3. **R CMD check still passes:**
   ```r
   devtools::check()
   # 0 errors, 0 warnings, 0 notes
   ```
</verification>

<success_criteria>
- [ ] R/delarr-seed.R contains @examples section
- [ ] man/delarr_seed.Rd contains \examples section
- [ ] Example demonstrates creating a seed with nrow, ncol, and pull function
- [ ] Example demonstrates wrapping seed in delarr() for lazy operations
- [ ] Example runs successfully in <5 seconds
- [ ] R CMD check still passes with 0 errors, 0 warnings, 0 notes
- [ ] DOCS-03 gap closed: all 19/19 exported functions now have @examples
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-06-SUMMARY.md`
</output>
