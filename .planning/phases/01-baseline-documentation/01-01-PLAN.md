---
phase: 01-baseline-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - .Rbuildignore
autonomous: true

must_haves:
  truths:
    - "R CMD check produces 0 NOTEs related to LICENSE or hidden files"
    - ".planning directory is excluded from package build"
  artifacts:
    - path: "LICENSE"
      provides: "Valid MIT license stub in DCF format"
      contains: "YEAR:"
    - path: ".Rbuildignore"
      provides: "Build exclusion patterns"
      contains: ".planning"
  key_links: []
---

<objective>
Fix the two R CMD check NOTEs identified in the baseline audit: invalid LICENSE stub format and hidden `.planning` directory.

Purpose: Establish a clean R CMD check baseline (0 errors, 0 warnings, 0 NOTEs) before adding documentation examples.
Output: LICENSE file with proper DCF stub format, .Rbuildignore updated to exclude .planning directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-baseline-documentation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LICENSE stub DCF format</name>
  <files>LICENSE</files>
  <action>
Replace the full MIT license text in LICENSE with a proper CRAN-compliant DCF stub format:

```
YEAR: 2024
COPYRIGHT HOLDER: Ben Buchsbaum
```

CRAN requires MIT LICENSE files to contain only YEAR and COPYRIGHT HOLDER fields (the full license text is implied by "MIT + file LICENSE" in DESCRIPTION). The current file has the full MIT text which triggers the "License stub is invalid DCF" NOTE.
  </action>
  <verify>
Run `R CMD build . --no-manual 2>&1 | grep -i license` - should show no warnings about invalid stub.
Alternatively: `cat LICENSE` should show exactly 2 lines with YEAR and COPYRIGHT HOLDER fields.
  </verify>
  <done>LICENSE file contains only "YEAR: 2024" and "COPYRIGHT HOLDER: Ben Buchsbaum" lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add .planning to .Rbuildignore</name>
  <files>.Rbuildignore</files>
  <action>
Add the `.planning` directory to .Rbuildignore to prevent the "Found the following hidden files and directories" NOTE.

Append this line to .Rbuildignore:
```
^\.planning$
```

The caret and dollar sign ensure exact matching of the directory name. This is the standard regex pattern for excluding directories from R package builds.
  </action>
  <verify>
Run `grep -c "planning" .Rbuildignore` - should return 1.
Run `R CMD check . --no-manual 2>&1 | grep -i "hidden files"` - should return nothing.
  </verify>
  <done>.Rbuildignore contains pattern to exclude .planning directory.</done>
</task>

<task type="auto">
  <name>Task 3: Verify clean R CMD check baseline</name>
  <files></files>
  <action>
Run R CMD check to verify both NOTEs are resolved:

```bash
cd /Users/bbuchsbaum/code/delarr
R CMD build . --no-manual
R CMD check $(ls -t *.tar.gz | head -1) --no-manual --as-cran 2>&1 | tail -30
```

Expected result: 0 errors, 0 warnings, 0 NOTEs (or only NOTEs unrelated to LICENSE/hidden files).

If any issues remain, document them for troubleshooting.
  </action>
  <verify>
R CMD check output shows "Status: OK" or "Status: 1 NOTE" where the NOTE is not about LICENSE or hidden files.
  </verify>
  <done>R CMD check produces no NOTEs related to LICENSE stub or hidden .planning directory.</done>
</task>

</tasks>

<verification>
- LICENSE file is valid DCF format (2 lines only)
- .Rbuildignore excludes .planning directory
- R CMD check --as-cran produces 0 NOTEs about LICENSE or hidden files
</verification>

<success_criteria>
1. `cat LICENSE` shows exactly "YEAR: 2024" and "COPYRIGHT HOLDER: Ben Buchsbaum"
2. `grep "\.planning" .Rbuildignore` returns a match
3. R CMD check --as-cran passes with 0 errors, 0 warnings, and no NOTEs about LICENSE or hidden files
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-documentation/01-01-SUMMARY.md`
</output>
